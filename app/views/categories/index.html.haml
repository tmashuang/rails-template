%h3.text-center Category Listing

%ul.list-group.col-md-4.col-md-offset-4
  - @categories.each do |category|
    %li.list-group-item
      %span.badge
        = category.products.count
      = link_to category.name, category_path(category.id)
